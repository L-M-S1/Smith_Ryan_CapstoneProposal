Nurse PCs should not have access to Admin permissions.

The router should prevent unnessesary connections.

Patient documentation should be securely housed, only allowed access when provided with credentials.

Credentials should be rotated once every sixty days.

HR should not have access to patient documentation.

HR and Doctor files should be kept separate in the shared file system.
